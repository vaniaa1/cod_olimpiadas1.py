# cod_olimpiadas1.pyef 
  def suma(a,b):  x = a+b
  return x

def resta(a,b):
  x = a-b
  return x
  
  print("Dame el primer número")
  a = int(input())
  print("Dame el segundo número:")
  b = int(input())
  print("La suma da",suma(a,b),"y la resta da",resta (a,b))

def multiplicación(a,b):
  x = a*b
  return x

def division(a,b)
 x  = a/b
 return x
  
print("Dame el primer número:")
a = int(input())
print("Dame el segundo número:")
b = int(input())
print("El resultado es", multiplicación(a,b),"y la división", division (a,b))

def conversion(kilometros)
 metros = kilometros * 1000 
 return metros 

kilometros = 10
metros = conversion(kilometros)
print("La conversion de metros a kilometros es", multiplicación (kilometros*1000))


def area_triangulo(base,altura):
  area = (base * altura) / 2
  return area
  base = 5
  altura = 10
  area = area_triangulo(base,area)
ptint("La area del triangulo es", multiplicación(altura ,base))
